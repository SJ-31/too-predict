[project]
name = "too_predict"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "anndata>=0.11.3",
  "ipython>=8.32.0",
  "pyhere>=1.0.3",
  "pytest>=8.3.4",
  "rpy2>=3.5.17",
  "scanpy>=1.10.4",
  "scikit-bio>=0.6.3",
  "scikit-learn>=1.6.1",
]

[tool.poetry]
name = "too-predict"
packages = [{ include = "tpre", from = "src" }]
